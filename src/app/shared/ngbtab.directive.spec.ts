import { NgbtabDirective } from './ngbtab.directive';

describe('NgbtabDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbtabDirective();
    expect(directive).toBeTruthy();
  });
});
